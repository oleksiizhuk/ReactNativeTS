import {StyleSheet} from 'react-native';

export const genericTemplateStyles = () => {
  return StyleSheet.create({
    container: {
      flex: 1,
      marginHorizontal: 16,
      marginTop: 20,
    },
  });
};
