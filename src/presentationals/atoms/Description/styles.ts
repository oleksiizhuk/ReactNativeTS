import {StyleSheet} from 'react-native';

export const descriptionStyles = () => {
  return StyleSheet.create({
    description: {
      fontSize: 14,
      lineHeight: 16,
      fontWeight: '500',
      color: '#040505',
    },
  });
};
