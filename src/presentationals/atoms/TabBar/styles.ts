import {StyleSheet} from 'react-native';

export const styleTabBar = () => {
  return StyleSheet.create({
    container: {
      flex: 1,
    },
    content: {
      alignItems: 'center',
    },
  });
};
